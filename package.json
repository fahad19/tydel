{
  "name": "tydel",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "lint": "eslint src/**/*.js test/**/*.js",
    "test": "mocha",
    "cover": "nyc --require babel-register mocha",
    "docs:clean": "rimraf _docpress",
    "docs:serve": "docpress serve",
    "docs:build": "docpress build",
    "docs:publish": "npm run docs:clean && npm run docs:build && cd _docpress && git init && git commit --allow-empty -m 'update docs' && git checkout -b gh-pages && touch .nojekyll && git add . && git commit -am 'update docs' && git push git@github.com:fahad19/tydel gh-pages --force"
  },
  "author": "Fahad Ibnay Heylaal",
  "license": "MIT",
  "devDependencies": {
    "babel": "^6.5.2",
    "babel-cli": "^6.10.1",
    "babel-eslint": "^6.1.2",
    "babel-preset-es2015": "^6.9.0",
    "babel-preset-stage-0": "^6.5.0",
    "babel-register": "^6.9.0",
    "chai": "^3.5.0",
    "docpress": "^0.6.12",
    "eslint": "^3.0.1",
    "mocha": "^2.5.3",
    "nyc": "^7.0.0",
    "rimraf": "^2.5.4"
  },
  "dependencies": {
    "lodash": "^4.13.1"
  }
}
